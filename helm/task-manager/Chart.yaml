apiVersion: v2
name: task-manager
description: >
  Task Manager Service — REST API for async task submission and status retrieval.
  Stores task state in PostgreSQL; dispatches work via AWS SQS FIFO queues.
type: application
version: 0.1.0
appVersion: "0.0.1"
