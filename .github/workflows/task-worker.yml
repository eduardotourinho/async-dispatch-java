name: task-worker CI/CD

on:
  push:
    branches: [master, main]
    paths:
      - "common/**"
      - "task-worker/**"
      - "helm/task-worker/**"
      - ".github/workflows/task-worker.yml"
  pull_request:
    paths:
      - "common/**"
      - "task-worker/**"
      - "helm/task-worker/**"
      - ".github/workflows/task-worker.yml"

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: task-worker

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          cache: gradle

      - name: Build and test task-worker
        run: ./gradlew :task-worker:test --no-daemon

      - name: Publish test report
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: task-worker Tests
          path: task-worker/build/test-results/**/*.xml
          reporter: java-junit

  helm-lint:
    name: Helm Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Lint Helm chart
        run: helm lint helm/task-worker

      - name: Template render (dev values)
        run: helm template task-worker helm/task-worker -f helm/task-worker/values-dev.yaml

      - name: Template render (prod values)
        run: helm template task-worker helm/task-worker -f helm/task-worker/values-prod.yaml

  docker-build-push:
    name: Docker Build & Push
    runs-on: ubuntu-latest
    needs: [build-and-test, helm-lint]
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build \
            -f task-worker/Dockerfile \
            -t $REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG \
            -t $REGISTRY/$ECR_REPOSITORY:latest \
            .
          docker push $REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $REGISTRY/$ECR_REPOSITORY:latest
